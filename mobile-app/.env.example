# Mobile App Environment Configuration
# Copy this file to .env and fill in your values

# Clerk Authentication
# Get these from https://dashboard.clerk.com
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxx

# Convex Database
# Get this from https://dashboard.convex.dev
EXPO_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# API Base URL
# For local development, use your local IP address (not localhost)
# Example: http://192.168.1.100:3000
# For production, use your deployed Next.js URL
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# Notes:
# - In Expo, environment variables must be prefixed with EXPO_PUBLIC_ to be accessible in the app
# - For local development, replace localhost with your computer's local IP address
# - You can find your local IP address by running `ipconfig` (Windows) or `ifconfig` (Mac/Linux)
# - The mobile app connects to the same Next.js backend as the web app
